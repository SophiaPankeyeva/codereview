# Використовуючи регулярні вирази, для поданого нижче тексту замініть кожне входження автомобільного номеру
# у форматі `АА 0000 BB` на текст `[номер АА]`. Виведіть результат.
import re
def main():
    n=("enter the number")

n= (^\w\w\ \d\d\d\d\ \w\w $)

print(n)